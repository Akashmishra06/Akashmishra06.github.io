# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# ─── Environment variables — NEVER commit these ───
# .env holds your real EmailJS keys — keep it local only
.env
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local
# NOTE: .env.example is intentionally NOT listed here — it's safe to commit

# misc
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
